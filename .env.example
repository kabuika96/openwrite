# Records API and production frontend: loopback-only, no sign-in
OPENWRITE_BACKEND_PORT=8787
OPENWRITE_RECORDS_PATH=./data/records

# Optional local converters
# OPENWRITE_OCR_LANGUAGES=eng
# OPENWRITE_DOCLING_COMMAND=/absolute/path/to/docling

# Frontend development server: loopback-only
OPENWRITE_FRONTEND_PORT=5173
# OPENWRITE_BACKEND_HTTP_ORIGIN=http://127.0.0.1:8787

# MCP adapter: loopback-only
# OPENWRITE_RECORDS_ORIGIN=http://127.0.0.1:8787
